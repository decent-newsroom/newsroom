{% extends 'app-shell.html.twig' %}

{% block sidebar %}
    <twig:SidebarNav
        id="leftNav"
        :sections="readingNookNav"
        :backLink="{label: 'nav.backToDiscover', route: 'discover', icon: 'iconoir:arrow-right'}"
        :footerComponent="'components/UserMenu.html.twig'"
    />
{% endblock %}

{% block aside %}
    {% if app.user %}
        <section class="context-panel">
            <p class="eyebrow">{{ 'nav.readingNook'|trans }}</p>
            <h2>{{ 'nav.discoverMoreContent'|trans }}</h2>
            <a class="btn btn--secondary" href="{{ path('discover') }}">{{ 'nav.browseArticles'|trans }}</a>
        </section>
    {% else %}
        {% include 'blog-journey/_login_cta_card.html.twig' %}
    {% endif %}
{% endblock %}
