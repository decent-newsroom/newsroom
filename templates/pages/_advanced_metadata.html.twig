{% block _editor_advancedMetadata_widget %}
    <div class="advanced-metadata-section" {{ stimulus_controller('content--advanced-metadata') }}>
                <div class="row hidden">
                    <div class="col-md-6">
                        {{ form_row(form.doNotRepublish) }}
                    </div>
                    <div class="col-md-6">
                        {{ form_row(form.license, {
                            'attr': {
                                'data-content--advanced-metadata-target': 'licenseSelect',
                                'data-action': 'change->content--advanced-metadata#licenseChanged'
                            }
                        }) }}
                    </div>
                </div>

                <div class="row hidden">
                    <div class="col-md-12">
                        {{ form_row(form.customLicense, {
                            'attr': {
                                'data-content--advanced-metadata-target': 'customLicenseInput'
                            }
                        }) }}
                    </div>
                </div>


                <h6>Zap Splits</h6>
                <p>Configure this when you want to share zaps with others. If nothing is set all zaps go to the npub that published the article by default.</p>
                <p class="text-muted small">Configure multiple recipients for zaps (tips). Weights determine the split ratio.</p>
                <p class="text-muted small">Don't forget to add yourself.</p>

                <div data-content--advanced-metadata-target="zapSplitsContainer"
                     data-prototype="{{ form_widget(form.zapSplits.vars.prototype)|e('html_attr') }}"
                     data-index="{{ form.zapSplits|length }}">
                    {% for zapSplit in form.zapSplits %}
                        <div class="zap-split-item mb-3" data-index="{{ loop.index0 }}">
                            <div class="row">
                                <div class="col-md-5">
                                    {{ form_row(zapSplit.recipient) }}
                                </div>
                                <div class="col-md-4">
                                    {{ form_row(zapSplit.relay) }}
                                </div>
                                <div class="col-md-3">
                                    {{ form_row(zapSplit.weight) }}
                                </div>
                            </div>
                            <button type="button"
                                    class="btn btn-sm btn-danger mt-2"
                                    data-action="click->content--advanced-metadata#removeZapSplit"
                                    data-index="{{ loop.index0 }}">
                                Remove
                            </button>
                            <div class="zap-share-display mt-2 text-muted">
                                <small>Share: <span class="share-percent">0</span>%</small>
                            </div>
                        </div>
                    {% endfor %}
                </div>

                <div class="d-flex gap-3 mb-3">
                    <button type="button"
                            class="btn btn-sm btn-secondary"
                            data-content--advanced-metadata-target="addZapButton"
                            data-action="click->content--advanced-metadata#addZapSplit">
                        Add Zap Recipient
                    </button>
                    <button type="button"
                            class="btn btn-sm btn-outline-secondary"
                            data-content--advanced-metadata-target="distributeEquallyButton"
                            data-action="click->content--advanced-metadata#distributeEqually">
                        Distribute Equally
                    </button>
                </div>


                <h6>Sources</h6>
                <p class="text-muted small">Add reference URLs for sources used in this article. These are published as <code>r</code> tags on the Nostr event.</p>

                <div data-content--advanced-metadata-target="sourcesContainer"
                     data-prototype="{{ form_widget(form.sources.vars.prototype)|e('html_attr') }}"
                     data-index="{{ form.sources|length }}">
                    {% for source in form.sources %}
                        <div class="source-item mb-3 d-flex align-items-center gap-2" data-index="{{ loop.index0 }}">
                            {{ form_widget(source) }}
                            <button type="button"
                                    class="btn btn-sm btn-danger"
                                    data-action="click->content--advanced-metadata#removeSource">
                                Remove
                            </button>
                        </div>
                    {% endfor %}
                </div>

                <div class="mb-3">
                    <button type="button"
                            class="btn btn-sm btn-secondary"
                            data-action="click->content--advanced-metadata#addSource">
                        Add Source
                    </button>
                </div>


                <h6>Media Attachments</h6>
                <p class="text-muted small">Attach media files with metadata. These are published as <code>imeta</code> tags on the Nostr event.</p>

                <div data-content--advanced-metadata-target="mediaAttachmentsContainer"
                     data-prototype="{{ form_widget(form.mediaAttachments.vars.prototype)|e('html_attr') }}"
                     data-index="{{ form.mediaAttachments|length }}">
                    {% for attachment in form.mediaAttachments %}
                        <div class="media-attachment-item mb-3" data-index="{{ loop.index0 }}">
                            <div class="row">
                                <div class="col-md-7">
                                    {{ form_row(attachment.url, {
                                        'attr': {
                                            'data-action': 'blur->content--advanced-metadata#guessMediaMimeType change->content--advanced-metadata#guessMediaMimeType'
                                        }
                                    }) }}
                                </div>
                                <div class="col-md-4">
                                    {{ form_row(attachment.mimeType) }}
                                </div>
                            </div>
                            <button type="button"
                                    class="btn btn-sm btn-danger mt-2"
                                    data-action="click->content--advanced-metadata#removeMediaAttachment">
                                Remove
                            </button>
                        </div>
                    {% endfor %}
                </div>

                <div class="mb-3">
                    <button type="button"
                            class="btn btn-sm btn-secondary"
                            data-action="click->content--advanced-metadata#addMediaAttachment">
                        Add Media Attachment
                    </button>
                </div>


                <div class="row hidden">
                    <div class="col-md-6">
                        {{ form_row(form.contentWarning) }}
                    </div>
                </div>
                <div class="row hidden">
                    <div class="col-md-6">
                        {{ form_row(form.expirationTimestamp, {
                            'attr': {
                                'data-advanced-metadata-target': 'expirationInput',
                                'data-action': 'change->advanced-metadata#expirationChanged'
                            }
                        }) }}
                    </div>
                </div>

                <div class="row hidden">
                    <div class="col-md-12">
                        {{ form_row(form.isProtected, {
                            'attr': {
                                'data-advanced-metadata-target': 'protectedCheckbox',
                                'data-action': 'change->advanced-metadata#protectedChanged'
                            }
                        }) }}
                        <div class="alert alert-warning mt-2"
                             style="display: none;"
                             data-advanced-metadata-target="protectedWarning">
                            <small><strong>Warning:</strong> Some relays may reject protected events. Use with caution.</small>
                        </div>
                    </div>
                </div>
    </div>
{% endblock %}

