{% extends 'layout.html.twig' %}

{% block body %}
    <section class="d-flex gap-3 center ln-section--newsstand">
        <div class="container mt-5 mb-5">
            <h1>{{ 'lists.heading'|trans }}</h1>
            <p class="eyebrow">{{ 'lists.eyebrow'|trans }}</p>
        </div>
    </section>
    <section class="mb-5">
        <twig:Organisms:ReadingListList />
    </section>
{% endblock %}

