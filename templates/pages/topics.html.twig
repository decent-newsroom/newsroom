{% extends 'layout.html.twig' %}

{% block ogtags %}
    {{ parent() }}
    <meta property="og:title" content="{% if selectedLabel %}{{ selectedLabel }} - {% endif %}Topics - Newsroom">
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ app.request.uri }}">
    <meta property="og:description" content="{% if selectedLabel %}Explore articles about {{ selectedLabel }}{% else %}Explore topics and discover articles on Newsroom{% endif %}">
    <meta property="og:site_name" content="Newsroom">

    {# JSON-LD Structured Data for Topics Page #}
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "CollectionPage",
        "name": "{% if selectedLabel %}{{ selectedLabel }}{% else %}Topics{% endif %}",
        "description": "{% if selectedLabel %}Explore articles about {{ selectedLabel }}{% else %}Explore topics and discover articles on Newsroom{% endif %}",
        "url": {{ app.request.uri|json_encode|raw }},
        "isPartOf": {
            "@type": "WebSite",
            "name": "Newsroom",
            "url": "{{ url('home') }}"
        }{% if selectedLabel and articles is not empty %},
        "about": {
            "@type": "Thing",
            "name": {{ selectedLabel|json_encode|raw }}
        },
        "mainEntity": {
            "@type": "ItemList",
            "numberOfItems": {{ articles|length }},
            "itemListElement": [
                {% for article in articles %}
                {
                    "@type": "ListItem",
                    "position": {{ loop.index }},
                    "item": {
                        "@type": "NewsArticle",
                        "headline": {{ article.title|json_encode|raw }},
                        {% if article.summary %}
                        "description": {{ article.summary|striptags|json_encode|raw }},
                        {% endif %}
                        "url": "{{ url('article-slug', {slug: article.slug}) }}"
                    }
                }{% if not loop.last %},{% endif %}
                {% endfor %}
            ]
        }
        {% endif %}
    }
    </script>
{% endblock %}

{% block body %}
<h1>{{ 'topics.heading'|trans }}</h1>
<p>{{ 'topics.selectTopic'|trans }}</p>

<div class="topics-list">
    {% for key, topic in topics %}
        <a href="{{ path('topics', {'topic': key}) }}" class="topic-button {{ selectedTopic == key ? 'active' : '' }}">
            {{ topic.name }}
        </a>
    {% endfor %}
</div>

{% if selectedLabel and articles %}
    <h2>{{ 'topics.recentIn'|trans({'%topic%': selectedLabel}) }}</h2>
    <twig:Organisms:CardList :list="articles" class="article-list" />
{% elseif selectedLabel %}
    <p>{{ 'topics.noArticles'|trans }}</p>
{% endif %}
{% endblock %}
