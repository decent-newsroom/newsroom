framework:
    default_locale: en
    enabled_locales: ['en', 'sl', 'de', 'es', 'fr']
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - en
        providers:
