# ChatBundle Routes - community subdomain chat
# ChatRequestListener validates subdomains dynamically against the database
# and sets _chat_community request attribute for registered community subdomains

chat_bundle:
    resource: '@ChatBundle/Resources/config/routes.yaml'

